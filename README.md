# immich-auto-dumper

Automatic backup tool for self-hosted [Immich](https://immich.app/) on Linux.

Two backup policies:

1. **Database backup** — copies PostgreSQL dumps generated by Immich to external storage and manages retention.
2. **Photo archiving** — when the `library/` folder exceeds a configurable threshold, moves the oldest photos to external storage while updating the Immich database, without any metadata loss.

## Requirements

- Ubuntu 22.04+ or Debian 12+
- `docker`, `jq`, `bc`, `curl`
- Immich deployed via Docker Compose
- External storage mounted locally and accessible by the `immich_server` container

## Installation

```bash
curl -fsSL https://raw.githubusercontent.com/de-seingalt/immich-auto-dumper/main/install.sh | bash
```

Installs to `/opt/immich-auto-dumper` and creates `/usr/local/bin/immich-auto-dumper`, then launches the configuration wizard.

To choose a different installation directory:

```bash
INSTALL_DIR=/opt/my-folder curl -fsSL .../install.sh | bash
```

## Configuration

```bash
immich-auto-dumper setup
```

The wizard detects active Docker containers, reads users from the database and builds the `storage_label → folder name` mapping on external storage. It can be re-run to update an existing configuration.

Values are written to `config.conf` (gitignored). See `config.conf.example` for the full structure.

### External storage structure

```
/mnt/external/
  .immich-backup/          ← DB dumps (mirror of UPLOAD_LOCATION/backups/)
  Alice/                   ← archived photos, per user
    2022/06/...
  Bob/
    2023/04/...
```

## Usage

```
immich-auto-dumper <command>

  setup     Interactive configuration wizard
  status    Service status, disk usage, last operations
  start     Enable cron jobs
  stop      Disable cron jobs, wait for current operation to finish
  dump_now  Force an immediate archive run down to the low threshold
  sync_now  Force an immediate copy of DB backups
```

## How archiving works

The archiving unit is the **monthly folder** (`<user>/<year>/<month>/`). A folder is never partially archived — if the free-space target is reached mid-folder, archiving completes the current folder before stopping.

For each file moved:
1. Copy to external storage
2. Verify the destination file exists
3. Update `original_path` in the database (transaction)
4. Verify accessibility from the `immich_server` container
5. Delete the source file

If any step fails, the database is rolled back to its previous state and the destination file is deleted. The source file remains intact.

A rescan of the external library is triggered via the Immich API at the end of archiving to refresh thumbnails, without creating duplicates or losing metadata.

## Logs

```
/var/log/immich-auto-dumper/immich-auto-dumper.log   # operations
/var/log/immich-auto-dumper/cron.log                 # cron output
```
